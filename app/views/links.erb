<h1> Bookmarks </h1>

<h2>Welcome, <%= current_user.email %> to the Bookmarks Manager</h2>

<ul id='links'>
  <% @bookmarks.each do |bookmark| %>
    <li>
      Title: <%= bookmark.title %>
      URL:   <%= bookmark.url   %>
      Tag:   <%= bookmark.tags.map(&:label).join(', ') %>
    </li>
  <% end %>
</ul>

<form action="/links/new">
  <input type="submit" value="Add link">
</form>

<p><%= User.all.count %> users</p>
